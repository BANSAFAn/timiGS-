{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "tray"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:allow-open",
    "process:default",
    "fs:default",

    "dialog:default"
  ]
}
